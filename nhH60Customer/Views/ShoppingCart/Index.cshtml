@model nhH60Customer.Models.ShoppingCart


<main>

    <div>
        @foreach (var item in Model.CartItems) {
            <div>
                @item.Product.Description
            </div>
        }
    </div>


</main>